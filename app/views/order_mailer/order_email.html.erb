<!doctype html>
<html lang="en">

  <head>
    <title>A Brandon Hire branch has placed an order for drinks</title>
    <meta charset="utf-8">
    <meta name="author" content="Brandon Hire - David Dean" />
    <meta name="description" content="Brandon Hire ordering system" />
  </head>
  
  <body>
    
    <h1>A Brandon Hire branch has placed an order for drinks</h1>

    <p style="color:f00;">This order has been approved by Brandon Hire head office and is ready to be processed.</p>
    
    <h2>Branch details</h2>
    <p>Branch No: <%= @order.user.depot_number %></p>
    <p>Branch Name: <%= @order.user.depot_name %></p>
    <p>Branch Address:</p>
    <p><%= @order.user.address1 %></p>
    <p><%= @order.user.address2 %></p>
    <p><%= @order.user.address3 %></p>
    <p><%= @order.user.address4 %></p>
    <p><%= @order.user.postcode %></p>

    <h2>Order details</h2>
    <ul>
      <% for li in @order.line_items %>
        <% unless li.quantity == 0 %>
          <li>
            <%= li.quantity %> x
            <%= li.product.name %> @
            &pound;<%= sprintf("%.02f", li.product.price / 100.0) %> each =
            &pound;<%= sprintf("%.02f", li.total / 100.0) %>
          </li>
        <% end %>
      <% end %>
    </ul>
    <p><strong>Order Total: &pound;<%= sprintf("%.02f", @order.total / 100.0) %></strong></p>
    
  </body>
  
</html>